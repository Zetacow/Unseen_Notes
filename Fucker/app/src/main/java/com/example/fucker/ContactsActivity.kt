package com.example.fucker

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

// ContactsActivity displays a list of contacts and their associated keys
class ContactsActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // setContentView(R.layout.activity_contacts) // You can create a layout for contacts
        // TODO: Load and display contacts and their keys
    }
}
